<div class="contents-header">
  <u>＜<%= @posts[0].user.nickname %>さんの投稿＞</u>
</div>

<% if @posts != nil %>
  <div class="contents row">
    <% @posts.each do |post| %> 
      <%= render partial: "posts/post", locals: { post: post } %>
    <% end %>
  </div>
<% end %>